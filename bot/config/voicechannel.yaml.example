# Only structural / data fields stay in yaml. All user-facing text
# (title / footer / description / button labels / status messages) lives
# in bot/locales/<lang>/voicechannel.yaml and is resolved via
# bot.utils.i18n.t('voicechannel.control_panel.*').
control_panel:
  colors:
    public: 3447003
    private: 15158332
